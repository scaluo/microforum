<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title,t(:title) %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :category,t(:category) %><br />
    <%= f.select :category_id,category_options(@categories) %>
  </div>
  <div class="field">
    <%= f.label :content,t(:content) %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit t(:submit)%>
  </div>

<% end %>
